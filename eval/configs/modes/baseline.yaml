# Baseline mode — no CodraGraph, pure mini-swe-agent (control group)
agent:
  agent_class: 'eval.agents.codragraph_agent.CodraGraphAgent'
  codragraph_mode: 'baseline'
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: 'docker'
