{
  "mcpServers": {
    "codragraph": {
      "command": "node",
      "args": [
        "-e",
        "const { spawn } = require('node:child_process'); const win = process.platform === 'win32'; const child = spawn(win ? 'cmd' : 'npx', win ? ['/c', 'npx', '-y', '@codragraph/cli@2.1.5', 'mcp'] : ['-y', '@codragraph/cli@2.1.5', 'mcp'], { stdio: 'inherit' }); child.on('error', (err) => { console.error(err.message); process.exit(1); }); child.on('exit', (code, signal) => process.exit(code ?? (signal ? 1 : 0)));"
      ]
    }
  }
}
